package com.neighbourhub.neighbourhub;

import com.neighbourhub.NeighbourhubApplication;
import org.springframework.boot.SpringApplication;

public class TestNeighbourhubApplication {

    public static void main(String[] args) {
       // SpringApplication.from(NeighbourhubApplication::main).with(TestcontainersConfiguration.class).run(args);
    }

}
