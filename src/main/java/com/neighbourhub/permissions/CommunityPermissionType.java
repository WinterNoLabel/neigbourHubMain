package com.neighbourhub.permissions;

public enum CommunityPermissionType {
    EDIT_DESCRIPTION,
    EDIT_NAME,
    WRITE_TO_CHAT,
    INVITE_USERS,
    KICK_MEMBER,
    CREATE_ROLES, MANAGE_ROLES, DELETE_ROLES, STREAM
}
